# Gemfile

# Bundler file
# Before using, and after updating this file:
# 1. delete: Gemfile.lock
# 2. run: bundle install

# Gem sources
source 'https://rubygems.org'

# The gems we require
# These will be downloaded if not present on the host system

gem 'scss_lint'
